package com.example.socialnetworkapp.repository;

/**
 * RepositoryType : enum
 *  - has all the repository types available at the moment
 *  - used for the RepositoryFactory class
 */
public enum RepositoryType {
    user,
    friendship,
    friendship_request,
    message
}
